import 'package:flutter/material.dart';

class TabelaDeAtualizacaoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tabela de Atualização'),
      ),
      body: Center(
        child: Text('Conteúdo da Tabela de Atualização'),
      ),
    );
  }
}