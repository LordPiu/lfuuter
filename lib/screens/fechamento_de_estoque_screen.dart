// fechamento_de_estoque_screen.dart

import 'package:flutter/material.dart';

class FechamentoDeEstoqueScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fechamento de Estoque'),
      ),
      body: Center(
        child: Text('Conteúdo da tela de Fechamento de Estoque'),
      ),
    );
  }
}